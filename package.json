{
  "name": "@tingswap/token-lists",
  "version": "1.0.0",
  "description": "Token lists typings, schema and lists used on Tingswap.",
  "main": "dist/index.js",
  "types": "index.d.ts",
  "files": [
    "dist"
  ],
  "repository": "https://github.com/tingxswapz/token-lists.git",
  "author": "tingswap",
  "scripts": {
    "start": "npx tsc --watch",
    "build": "npx tsc --build",
    "prepublishOnly": "npx tsc --build",
    "test": "jest"
  },
  "license": "MIT",
  "publishConfig": {
    "access": "public"
  },
  "devDependencies": {
    "jest": "^29.5.0",
    "typescript": "^4.8.2"
  }
}
